# 安全问题

本项目能够触发真实硬件输出。强度、时长、停止、设备选择、鉴权、证书或网络边界问题应按安全问题处理。

不要在公开 Issue 中披露可直接利用的细节、私钥、token、局域网地址或复现二维码。优先使用 GitHub 仓库的 Private vulnerability reporting；该入口不可用时，通过维护者已有的私下联系方式报告。

报告至少包含受影响版本、平台、设备型号、触发步骤、预期结果、实际结果，以及是否发生真实输出。不要为了复现问题提高强度或延长时间。

本项目不承诺固定响应时限。存在持续输出或停止失效时，应立即停止使用，并在设备 App 中人工清空输出。
