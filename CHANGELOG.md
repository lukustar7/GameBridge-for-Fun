# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- 电脑控制台新增独立“安全测试”页签，自检、试电和停止操作由二维码长页底部移到单屏可见区域；在 `1165×825` 审计视口中无需滚动即可完成全部测试操作。
- 五个玩法新增统一运行阶段栏，覆盖“输出中、休息中、间隔中、等待判定、已暂停、已停止”6 类可感知状态；长时间任务不再表现为无响应。
- 骰子新增前后端双重单局记账，按局号和顺序限制最多 `36` 下、累计真实输出最多 `300 秒`，休息间隔不计入预算。

### Changed

- 手机游戏首页调整为“全局设置、选择玩法、说明与排障”3 个任务页签，仍默认打开“选择玩法”；5 个玩法入口增加独立 SVG 图标，移动端统一为单列两行卡片。
- 五个游戏的参数分别归并为 `2–3` 个分类页签，全局波形与 A/B 通道仍只保留 1 份；电脑技术信息由 1 张 24 行长表拆为 3 栏，在 `1165×825` 审计视口中页面高度由 `1409` 降至 `949` 像素，减少 `33%`。
- 骰子每下范围由 `1–5 秒` 扩展为 `1–30 秒`，次数上限调整为规则可产生的 `36` 下；角子机满槽惩罚扩展到 `30 秒`，基础输出与轻电规则重新归类。
- 雷电极速行驶输出和休息均扩展到 `30 秒`，超速恢复调整为 `0–10 秒`，单局时长改为 `1–30 分钟` 滑块，堵车单次输出扩展到 `20 秒` 且开关默认关闭。
- 手机与电脑安全试电默认时长由 `0.3 秒` 延长为 `1 秒`，强度继续使用 `15`，后台最高时长仍锁定为 `1 秒`。

### Fixed

- 修复手机“全局设置”页中设置卡片边框相贴的问题；3 张设置卡片的垂直间距统一为 `15` 像素。
- 修复 4 张设备能力卡片高度和按钮底线不齐的问题；`378×819` 移动视口实测卡片高度均为 `154` 像素、按钮高度均为 `44` 像素。
- 修复“全局输出设置”缺少展开方向提示、雷电极速安全弹窗偏离视口中心的问题；移动视口实测弹窗横纵中心偏差均为 `0`。
- 修复手机设置页底部操作栏覆盖滑块说明的问题；恢复默认、校准和开始按钮改为参数上方的正常页面内容，实测与首个参数分组保持 `14` 像素间距且无重叠。
- 游玩页返回按钮改为“结束游戏并返回”，明确该操作会先停止当前输出和预约任务，避免用户把它误认为普通页面返回。
- 移除手抖挑战和保持角度中含义不成立的“电完休息”控件，改用内部固定控制节奏；夹缝内圈半径仅在选择夹缝模式时显示。
- 修复迟到的骰子或角子机输出回执可能影响随后进入的另一玩法；回执现按请求号和玩法关联，切换游戏时清空旧关联。

### Security

- 雷电极速长输出期间每秒提交脱敏速度状态，后台只接受当前局号、低于 `60 km/h`、不超过 `3 秒`的有效样本；连续 `2.5 秒`没有合规样本即停止 A/B。
- 后台拒绝缺少玩法和阶段的通用结算输出，角子机与骰子分别锁定各自时长边界；达到 `60 km/h`的持续样本会在长任务进行中立即触发硬停止。

## [1.1.0-beta.1] - 2026-07-27

### Added

- 新增第 5 个玩法“雷电极速”，使用 8 种显式状态处理等待、行驶、低速、堵车、超速、定位异常和单局结束；速度达到启动线并稳定 2 秒后才运行，行驶强度按速度线性变化。
- 新增 1 个全局设备能力中心，统一检查动作与方向、GPS/GNSS 速度、屏幕常亮和本机震动共 4 类能力；权限被拒绝、系统开关关闭或设备缺少硬件时提供可重复检查的明确提示。
- Android APK 新增原生 GPS/GNSS 速度通道与精确定位权限恢复流程；原生层以 500 毫秒间隔读取速度，只向受限游戏页传入速度、精度和时间，不传入经纬度。

### Changed

- 骰子每下、角子机满槽惩罚和角子机轻电共 3 类单次结算输出统一为最低 `1 秒`；旧版 `0.x 秒` 本地设置会自动迁移，后端同时强制下限，避免单次输出短到难以感知。持续型玩法的可中断控制帧与 `0.3 秒` 安全试电保持不变，以保留快速停机能力。
- 波形、A/B 通道、B 路模式和比例改为 1 份全局输出设置，替代 4 个旧玩法各自保存的 12 个重复通道字段；新增玩法同样只读取全局设置，5 个玩法切换时不再静默改变接线目标。
- 公开 Beta 版本由 `1.0.0-beta.1` 升至 `1.1.0-beta.1`，Android 升级序号由 2 增至 3；Python、锁文件、APK、用户文档和页面状态继续使用同一版本来源。

### Fixed

- 修复手动骰子仍会请求摇晃权限的问题；开启手动摇号后不再触发无关传感器授权，拒绝动作权限也能继续使用该模式。
- 修复浏览器可能用普通网络定位位移估算速度的问题；雷电极速现在必须连续取得系统直接提供的速度，无法提供可靠 GPS/GNSS 速度的设备保持禁用。
- 修复速度刚低于用户启动线时可能在 1 km/h 迟滞区继续输出的问题；停止线收紧为用户设置值，恢复仍需高出 1 km/h 并稳定 2 秒。

### Security

- 旧版 4 份通道设置仅在完全一致时自动迁移；存在冲突、损坏或未确认状态时，正式游戏输出保持关闭，直到用户重新核对接线并确认全局设置。
- 雷电极速每次开始均显示 4 项不可持久化安全确认，明确禁止驾驶员、骑行者或载具操作者成为接收者，并限制为封闭、清场的非公共区域。
- 速度达到 `60 km/h`、定位样本超过 `3 秒`、定位精度不足、页面退后台、单局到时或速度低于启动线时立即请求停止；超速恢复需连续低于硬停止线 `5–10 秒`。
- 后端独立限制雷电极速强度最高 `100`、行驶单轮 `3–8 秒`、行驶休息 `3–10 秒`、堵车单次 `1–3 秒`，并再次拒绝 `60 km/h` 及以上或超过 `3 秒`的定位样本。
- 网页端不再读取经纬度计算速度，Android 原生层也不向 JavaScript 或电脑发送经纬度；定位仅在能力检查或雷电极速运行期间开启，离开页面和切后台时停止。

## [1.0.0-beta.1] - 2026-07-23

### Added

- 新增手抖挑战、保持角度、摇骰子对决和极速角子机 4 种玩法，电脑控制台、手机网页和 Android APK 共用同一套可测试规则。
- 新增游戏默认、按时长随机与 16 种经典波形共 18 个输出感觉选项；短任务自动压缩完整波形，避免 0.5 至 1 秒输出只播放缓慢起始段。
- 新增 DG-LAB V4 Socket 本地网关，同时识别 `COYOTE_020` 与 `COYOTE_030`，按设备类型发送 V2 三字节或 V3 八字节波形。
- 新增 macOS 双击启动体检，覆盖系统、Python 3.9+、11 项运行文件、依赖、网络、OpenSSL、证书和 APK 完整性。
- 新增 Android 15+ 原生传感器与受限 WebView，提供常驻原生停止入口，切入后台或锁屏时主动断开并请求停止。
- 新增正式 Android 签名创建、发布构建和只读验签入口；版本、签名、SHA-256 与 APK 内部清单均进入发布检查。
- 新增 PolyForm Noncommercial 1.0.0 许可、第三方组件说明、贡献说明和安全问题报告流程，使公开源码能够在非商业目的下修改和再分发。
- 新增版本与发布卫生回归测试；Python 自动化用例由 72 项增加到 83 项，另保留 19 项浏览器规则测试和 12 项 Android 单元测试。

### Changed

- 首次公开版本统一为 `1.0.0-beta.1`；Python 元数据、uv 锁文件、Android `versionName`、APK 文件名、用户手册和页面状态使用同一版本来源。
- Python 后端集中到 `server/`，网页保留在 `static/`，Android 与测试继续独立分区；完整源码、锁定依赖和构建脚本全部保留。
- 电脑控制台改为状态、证书和技术信息 3 个页签，手机端改为选择、设置和游玩 3 段流程，4 种玩法首次可见设置均压缩到最多 1.80 屏。
- 设备桥接从旧 V3 第三方依赖改为项目内 V4 实现，运行依赖从 2 个缩减为固定版本的 `websockets` 1 个。
- 安全试电固定使用立即有感波形，默认强度由 5 调整为 15；后端最高强度 30、最长 1 秒和 1.5 秒冷却保持不变。
- README 改为面向公开 Beta 的项目说明，只保留定位、4 种玩法、启动、APK、证书、安全、二次开发和许可信息。
- HTTPS 私钥从源码目录迁移到 `~/Library/Application Support/GameBridge for Fun/`，更新或移动项目文件夹不再复制私钥。

### Removed

- 移除 4 份逐字复制的上游 V2/V3 协议文档，改为 1 份官方资料索引，减少公开仓库中的授权歧义且不影响运行代码。
- 公开 APK 不再使用 Android Debug 证书，调试构建也不再覆盖 `APK/` 中的正式 Beta 安装包。

### Fixed

- 修复旧 V3 二维码只能添加 3.0 设备的问题，绑定、设备发现、强度和波形下发全部使用 V4 通道。
- 修复重复发送平直帧导致 2.0 设备低强度下几乎无感的问题，0.3 秒默认任务现在包含 3 个有效变化帧。
- 修复 App 限幅未读取时可能回退到协议最大强度的问题；任一所选通道状态未知、限幅为 0、静音或过热时整次输出为 0 条。
- 修复页面关闭、锁屏、弱网半断开、传感器停更和多页面同时断开时可能残留输出的问题，前后端统一取消预约任务并逐路清空 A/B。
- 修复角子机 `7️⃣` 与自带 777 图案含义混淆的问题；特殊事件只在三个图标全部为 `7️⃣` 时成立。
- 修复浏览器设置损坏、重复快速点击、迟到 WebSocket 回调和骰子并行结算可能绕过正常流程的问题。

### Security

- 非零强度全部使用带自动到期时间的 V4 临时任务；电脑意外退出后，App 仍会在请求时长加 0.8 秒安全余量内归零。
- 输出期间要求所属页面持续发送应用层心跳；长任务 3.5 秒无心跳、持续玩法 0.75 秒无新脉冲或传感器 1.6 秒无数据时强制停止。
- 后端强制限制强度 0 至 200、单次时长 100 至 60000 毫秒、最短脉冲间隔 0.22 秒，并拒绝重叠任务和未知设备型号。
- 游戏端使用每次启动随机 token；电脑控制台只接受本机来源，手机状态包不包含 App 配对二维码、运行 token 或证书详情。
- HTTP 公开范围限制为网页资源和 2 个根证书下载文件，拒绝源码、Git 数据、目录列表、私钥、路径穿越与符号链接。
- 根证书有效期限制为 90 天、服务器证书限制为 7 天；证书与私钥目录权限收紧为当前 macOS 用户可访问，符号链接证书在启动前拒绝使用。
- Android WebView 拒绝公网、文件、Cookie、弹窗、混合内容和非白名单资源；公开 APK 使用独立 4096 位 RSA 正式签名。
