# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- 新增第 4 款独立小游戏“极速角子机”，支持 3 轮 Emoji 高频开奖、彩色压力进度条、满槽惩罚和自动连转冷却，避免改动原有快节奏摇骰子玩法。
- 新增极速角子机“没中奖轻电”开关和轻电时长设置，使“没中奖轻电强度”只在空转轻惩罚开启时生效，避免满槽规则下参数无实际用途。
- 新增每个游戏 3 项输出通道设置，可选择只用 A、只用 B 或 A+B 同时输出，并支持 B 通道按 A 强度比例降低或保持一致。
- 新增极速角子机 14 个独立设置字段，覆盖轻电、满槽强度、满槽时长、开奖速度、自动连转、压力涨跌、中奖率和三个 7 特殊规则，使压力条节奏和惩罚强度可按玩法单独调校。
- 新增手机游戏选择页安全说明、A/B 通道说明和 13 项技术状态字段，使手机端可直接核对端口、连接、延迟、强度、限幅与设备电量接入状态。
- 新增手抖挑战、保持角度、摇骰子对决主要玩法字段的小字说明，明确每个强度、容错、灵敏度、时长和难度参数的用途，降低误调参数的概率。
- 新增电脑控制台的手机游戏端连接状态与设备电量接入状态展示；当前桥接库未暴露电量读取时显示“未接入”，避免把未知状态误报为正常电量。
- 新增手机端三段式游戏流程：选择游戏、独立设置、开始游玩，四个游戏的强度、容错、灵敏度、时长和压力参数互不污染。
- 新增游戏参数本地持久化，刷新页面后仍保留每个游戏的独立配置，减少重复调参成本。
- 新增手抖挑战与保持角度的姿态校准入口，并在开始游戏时自动兜底校准，降低初始握持角度导致的误触发。
- 新增游戏端 WebSocket 运行 token，手机二维码携带 `ws` 端口和 token，端口自动递增后仍可正确连接。
- 新增手机端页面隐藏、离开、刷新和冻结时的紧急停止逻辑，并请求屏幕常亮以降低游戏中途息屏概率。
- 新增摇骰子对决简化豹子规则，玩家自己摇出豹子时按“点数 × 豹子倍率”换算惩罚次数，并受单局最多次数限制。
- 新增每个游戏游玩页顶部说明，使玩家进入玩法后能立即看到结算规则和触发条件。

### Changed
- 后端状态广播从仅电脑控制台扩展到电脑和手机游戏页两端同步，技术排障入口从 1 个页面扩展为 2 个页面。
- 简化摇骰子对决结算逻辑，改为“输几点就电几下、每下固定强度和时长”，减少强度区间和时长区间造成的理解成本。
- 重写手机选择页、设置页和当前参数摘要的主要说明文案，将硬件术语压缩为玩家可直接理解的 A/B 输出、每下强度、没中奖轻电和满槽电击。
- 将 `pydglab-ws` 依赖锁定到已验证的 `1.1.0`，降低上游接口变更导致运行结果漂移的风险。

### Fixed
- 修复 B 通道选择“跟 A 一样”时仍显示比例滑块的问题，并在官方 App 未绑定时明确提示“未输出”，避免玩家误以为已经实际触发。
- 修复无 token 打开手机游戏页被拒绝时后端打印 WebSocket 异常栈的问题，使无效访问仍被拒绝但服务日志保持可读。
- 修复未绑定官方 App 时游戏页断开仍尝试发送硬件停止指令的问题，减少无设备测试和服务退出时的误导性错误日志。
- 修复手机游戏页全局禁用触摸滚动导致选择页和长设置页难以滚动的问题，并在页面切换时自动回到顶部，避免返回按钮出现在视口外。
- 修复本机局域网 IP 可能被识别为 `198.18.*` 虚拟网卡地址的问题；二维码现在优先使用 Wi-Fi/局域网私有地址，降低手机扫码后打不开页面的风险。
- 修复本地 `qrcode.min.js` 文件语法损坏导致电脑后台无法绘制二维码的问题，并提高二维码容量配置以容纳 App 绑定长链接。
- 修复二维码库初始化失败时阻断控制台 WebSocket 连接的问题；即使二维码无法绘制，页面也会显示可手动输入的绑定数据和游戏地址。
- 修复电脑后台二维码区域在 WebSocket 端口变更、浏览器缓存旧脚本或二维码生成异常时静默空白的问题；控制台现在会使用启动时的真实 WS 端口并显示离线/生成失败提示。
- 修复 V3 脉冲数据结构错误，将无效四元组改为 `pydglab-ws` 接受的双四元组波形，恢复实际脉冲下发链路。
- 修复 App 绑定二维码格式，改为使用库内 `get_qrcode` 生成，避免扫码入口与当前库协议不一致。
- 修复保持角度游戏启动即惩罚的问题，加入传感器就绪判断、启动缓冲和持续偏离判定。
- 修复传感器监听重复绑定、骰子手动摇号可连点、停止输出后仍可被运动传感器触发的问题。
- 修复 README 指向已删除硬件协议目录的问题，使项目说明与当前仓库结构一致。
- 修复手机游戏端断开连接后服务端没有立即兜底停止输出的问题，降低上滑回桌面或浏览器杀后台时残留输出风险。

### Security
- 后端新增强度 0-200、单次时长 100-60000ms、连续惩罚 0.22s 间隔的硬限制，降低局域网刷包导致的过量输出风险。
- 新增 `stop_shock` 停止输出路径，返回列表或点击停止输出时会清空 A/B 两路波形并尝试将强度归零。
- 将后端单次惩罚时长上限扩展到 60 秒，同时继续保留服务端硬限幅，避免前端异常输入无限延长输出。

## [1.0.0] - 2026-07-06

### Added
- 新增一键双击启动脚本 `start.command`，实现 macOS 环境下的依赖自检与服务自动拉起。
- 新增局域网 Web/WebSocket 控制台，支持展示 App 连接二维码、局域网 IP 与延迟 RTT 监控。
- 新增三个极简 Canvas/Web 网页小游戏（手抖挑战、保持角度、摇骰子对决）。
- 前端游戏集成 Web Audio API 骰子撞击音效合成器与 `navigator.vibrate` 物理震动反馈。
- 引入本地 `qrcode.min.js`，实现离线/弱网二维码极速渲染。
- 后端 Python 异步服务端，实现多服务端口防冲突分配与 V3 脉冲波形下发逻辑。

### Removed
- 删除了与郊狼脉冲主机无关的其他硬件协议文档（爪印无线按钮传感器、灵猫边缘控制传感器、负鼠振动控制器）。
