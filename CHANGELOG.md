# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- 新增 3 项四游戏整链路无硬件彩排，覆盖 A/B 分路限幅、重叠拒绝、令牌鉴权、页面断线和急停确认；界面结构测试同步增加 1 项，Python 自动化用例总数从 50 项增加到 54 项。
- 新增 macOS 启动前体检模块，单次启动覆盖系统、Python、10 项必需文件、写入权限、锁定依赖、OpenSSL、局域网、TLS 证书和 APK 校验 9 类检查；新增 10 项回归测试后 Python 自动化用例总数从 34 项增加到 44 项。
- 新增项目专用 `.venv` 自动创建与损坏环境留档机制，将 Python 依赖从用户全局环境隔离为 1 个项目目录，并在无 Python、只读目录、网络失败等场景保留中文处理提示。
- 新增内置 DG-LAB V4 Socket 网关与设备状态机，正式支持 `COYOTE_020`、`COYOTE_030` 两类硬件，并按型号发送 3 字节 V2 或 8 字节 V3 波形，使受支持郊狼型号从 1 类增加到 2 类。
- 新增控制目标选择、设备型号、蓝牙状态、静音、过热、电量和 A/B 安全状态界面；单设备自动选择，多设备强制人工确认，消除默认控制列表第一台设备的歧义。
- 新增 12 项 V4 回归测试，覆盖真实 WebSocket 握手往返、错误配对 ID、设备快照、双设备选择、限幅、过热停止和官方 X/Y/Z 小端编码；配合并发断开测试，Python 自动化用例总数从 21 项增加到 34 项。
- 新增浏览器与 Node 共用的纯游戏规则模块及 16 项行为测试，自动化覆盖从仅语法检查扩展到配置恢复、传感器时效、骰子边界、角子机概率分类和压力状态转换。
- 新增根目录 `verify.command` 五阶段本地验收入口，单次执行即可完成 Python、浏览器规则、Android、完整服务冒烟和 Git 差异检查，将 5 类验收收敛为 1 条命令。
- 新增低强度真实设备故障演练清单，覆盖锁屏、Wi-Fi 断开、页面关闭、`Ctrl+C` 和终端关闭 5 种现场异常。
- 新增 7 张不含真实二维码、IP、证书或 token 的本地 SVG 操作插图，覆盖三端协作、六步启动、双二维码、证书选择、游戏流程、紧急停止和五步排障，将首次使用指引从纯文字扩展为图文双通道。
- 新增根目录 `APK/` 交付目录和 SHA-256 校验文件，将安装包查找路径从 5 层 Gradle 构建目录缩短为 1 个固定目录；一键构建会自动覆盖 APK 与校验值。
- 新增 Android 原生加载中、连接失败和重新连接状态，电脑服务未启动、地址过期或 Wi-Fi 不通时不再显示无反馈空白页。
- 新增原创“闪电桥”Android 自适应图标及单色主题图标，替换仅显示字母的临时占位图。
- Android 单元测试从 5 项增加到 12 项，覆盖重复/多余字段、回环地址、前导零歧义 IPv4、畸形深链和去重力加速度滤波。
- 新增 6 项界面结构回归测试，锁定唯一元素 ID、手机紧急停止、四种玩法默认折叠、控制台页签语义、Android 原生停止入口和角子机图标文案；Python 自动化用例总数从 44 项增加到 50 项。

### Changed
- macOS 双击启动改为仅在依赖缺失或版本变化时联网，首次安装优先使用清华 PyPI 镜像；清华镜像失败后必须由用户明确确认才会尝试官方 PyPI。
- 设备桥接从只支持郊狼 3.0 的第三方 V3 库迁移为项目内 V4 实现，运行依赖从 `pydglab-ws` 与 `websockets` 2 个缩减为仅 `websockets` 1 个，同时保留电脑控制台、网页游戏和 Android APK 的原有数据通道。
- 输出调度改用 V4 临时强度与任务式波形：非零强度和波形并发入队，停止时清任务并逐路归零，兼容 V4 回执只在任务结束时返回的行为。
- 重做电脑控制台与手机游戏页的响应式视觉层级，二维码统一改为深色码白色底；手机首屏将 4 个游戏入口提前到连接详情和安全长文之前，减少开始玩法所需滚动距离。
- Android 游戏页改用独立 Material 工具栏、深浅色资源和 Android 15 系统边距适配，返回入口不再悬浮覆盖网页内容。
- 电脑控制台将 Android APK 调整为推荐主入口，增加网页与 APK 地址复制反馈，并将不支持热切换的端口编辑区改为只读连接说明。
- 启动脚本在 `websockets 12.0` 已匹配时跳过联网安装，将正常启动的依赖网络请求从每次 1 次降为 0 次。
- 手机选择页改为 2×2 玩法入口并把连接自检提前；四套设置按基础与高级规则分组，首次可见内容从最多 3.52 屏降至最多 1.80 屏，底部操作固定为单行。
- 电脑控制台将三个页签和 A/B 停止按钮合并为置顶操作栏，并补齐左右方向键、Home、End 与读屏页签语义；手机游玩页参数摘要默认折叠。
- Android 连接页和 WebView 统一为深色主题，游戏工具栏新增原生 A/B 停止入口；连接期间由原生层保持屏幕常亮，网页标题不再与原生标题重复显示。
- 角子机移除自带 777 图案的 `🎰` 并改用 `🍀`；“进入满槽”改为本局不输出、下一局没中奖才惩罚，使 3 个特殊事件选项从 2 种实际效果增加到 3 种。

### Fixed
- 修复服务启动后前 0.22 秒可能把第一条持续玩法脉冲误判为重复请求的问题；首次脉冲现在不再依赖事件循环的起始时钟值。
- 修复 macOS 开启 VPN 或虚拟网卡后可能把不可由手机访问的地址写入二维码和 HTTPS 证书的问题；地址探测现在优先默认物理 `en` 网卡并过滤 8 类常见虚拟接口前缀。
- 修复 OpenSSL 仅依赖终端 `PATH` 查找的问题；现在依次兼容 macOS 系统、Apple Silicon Homebrew、Intel Homebrew 和自定义路径共 4 类位置。
- 修复 DG-LAB 4 App 扫描项目二维码后进入 V3 页面、只能添加郊狼 3.0 的根本兼容问题；二维码、握手、设备发现和波形下发现已统一切换到 V4。
- 修复多个游戏网页同时断开时 Python 3.9 全局异步锁可能引用旧事件循环的问题；锁改为在服务主循环内创建，并增加并发断开测试保证停止动作串行完成。
- 修复总体验收只编译 Android 工程、未刷新 `APK/` 交付包的问题；当前安装包会包含 `game-logic.js` 白名单，构建产物与交付文件从哈希不一致改为逐字节一致，并强制通过 SHA-256 与 APK 签名校验。
- 修复持续型玩法在方向传感器停止更新后仍使用最后一帧坏姿态重复处罚的问题；数据超过 1.6 秒未更新时立即请求停机并清除旧出界计时。
- 修复损坏、越界类型或多余字段的浏览器本地设置可直接混入运行配置的问题；恢复逻辑现在只接纳默认结构中的已知字段和合法类型。
- 修正用户手册中的控制台页签名称、APK 二维码扫描主体、手机自检按钮名称和重复问题编号共 4 处文字错误，避免用户在错误设备或错误入口执行操作。
- 修复手机网页仅在正式游戏运行时执行页面隐藏停机的问题；现在安全试电、选择页和游戏页切后台都会请求停止并关闭连接，覆盖率由部分运行状态扩展到全部页面状态。
- 修复缺少旋转矢量但存在虚拟线性加速度传感器的 Android 设备无法得到倾斜方向的问题；兜底路径现在强制使用加速度计计算方向。
- 修复 Android 无效深链在游戏画面上被遮住、连接失败只弹短暂提示和返回按钮覆盖网页右上角操作的问题。
- 修复控制台和手机试电在 App 未绑定或所选 A/B 限幅未读取时仍发送请求的问题，前端现在会在请求进入网络前直接拒绝并说明原因。
- 修复弱网重连中旧 WebSocket 回调误关新连接的问题，并阻止迟到事件创建并行重连链。
- 修复 Android 普通加速度计把静止重力误判为摇晃的问题；第一帧只建立重力基线，后续骰子判定改用去重力动作分量。
- 修复硬件停止命令无超时且失败后仍显示强度 0 的问题；每条硬件命令最多等待 1 秒，未确认通道显示“未读取”，另一通道仍继续停止。
- 修复电脑状态刷新重复重画 6 个二维码、滑块拖动逐帧重建整张设置表单的问题；地址未变化时二维码重绘次数降为 0，表单只在松手后归一化 1 次。
- 修复感应器权限请求可被快速连点并发、骰子惩罚期间仍能再次摇号、输出失败仍继续假倒数和角子机未实际输出却清空压力的问题。
- 修复保持角度设置把强度 `15/70` 错误显示为 `15°/70°` 的单位混淆；数值格式改为共用纯函数并新增第 17 项浏览器规则测试。
- 修复顶部安全栏仅凭硬件蓝牙连接就显示绿色“已就绪”的误导状态；现在后台、硬件和所选 A/B 限幅必须全部有效，并以第 18 项浏览器规则测试锁定判定边界。
- 修复技术信息页把连接方向符号显示为原始 LaTeX 文本的问题，并同步修正界面缓存版本和使用手册入口说明。
- 修复“三个 7”无法区分 `7️⃣` 与 `🎰`、以及“立即最大惩罚”未说明强度来源的问题；界面现在明确限定为三个图标全部是 `7️⃣`，并区分用户设定值、B 通道比例与 App 硬件限幅。

### Security
- 手机置顶停止从只清理当前硬件任务改为同时取消 9 类游戏循环与延时句柄、清空 A/B 并退回玩法列表，阻断骰子最多 60 下待执行队列和角子机延时任务在急停后重新输出。
- 启动前新增 TLS 引擎证书与私钥配对校验和 APK SHA-256 校验；证书损坏会在开放 5 个网络监听端口前阻断启动，APK 损坏则明确标记为不可确认完整性。
- V4 非零强度全部设置自动到期时间，期限为请求时长加 0.8 秒安全余量；电脑异常退出时 App 仍会自动归零，紧急停止继续执行清任务和 A/B 归零双重保护。
- 后端在每次硬件写入前重新核验设备连接、型号、App 限幅、静音与过热状态；任一状态未知或危险时拒绝输出，多设备未确认目标时输出路径为 0 条。
- 新增双模式输出看门狗：长时结算超过 3.5 秒没有所属页面续报时自动停机，持续型玩法超过 0.75 秒没有新脉冲时自动归零且普通 ping 不能续期；后端故障注入测试从 17 项增加到 21 项。
- `Ctrl+C`、终端关闭和系统结束进程统一接入 3 类可捕获信号的安全退出路径，完整服务冒烟测试会实际发送 `TERM` 并验证停机流程完成。
- Android WebView 将主文档限制为规范化游戏 URL，子资源从整个 `/static/` 目录收紧为 `game-logic.js`、`game.js` 与 `style.css` 3 个文件，并拒绝重复、未知或被篡改的配对参数。
- HTTP 响应新增内容安全策略，慢连接限制为 10 秒且请求等待队列限制为 32；证书 IP 只接受规范 RFC1918 IPv4，阻断 OpenSSL 配置污染和无界请求线程占用。
- 游戏状态包移除设备 App 配对二维码、运行 token 和证书详情，敏感字段的接收范围由电脑与手机两端缩至仅本机控制台。
- 后端回归测试从 12 项增加到 17 项，覆盖证书 IP、端口越界/占用、状态最小披露和单通道停止超时。
- 电脑和手机浏览器新增跨页面常驻停止入口；Android 工具栏即使处于加载错误遮罩也会尝试执行停止，减少用户必须返回指定页面才能停机的路径。
- 损坏或未知的角子机特殊规则不再回退成立即输出，而是进入满槽并延后结算；浏览器规则测试从 18 项增加到 19 项。

## [1.1.0] - 2026-07-13

### Added
- 新增 Android 15+ 原生 APK，复用现有网页游戏逻辑并通过 `SensorManager` 提供方向与动作数据，使 Android 传感器玩法不再依赖用户安装 HTTPS 根证书。
- 新增 APK 专用 `gamebridgeforfun://connect` 配对二维码和手动地址兜底，电脑每次启动可把 IP、WS 端口及一次性 token 一次性交给 APK。
- 新增 5 项 Android 地址边界测试，覆盖私有局域网、配对深链、公网拒绝、错误路径、缺失 token 和损坏转义输入。
- 新增 `tests/test_server.py` 自动化回归测试，以 12 项用例覆盖 HTTP 公开边界、控制台来源鉴权、token 异常输入、真实硬件限幅读取、非法数值、A/B 限幅、单任务输出与双通道停止。
- 新增 `AGENTS.md` 仓库贡献指南，以 1 份文档集中覆盖项目结构、常用开发与校验命令、代码风格、测试要求、提交规范及安全边界，减少新贡献者查找分散约定的成本。
- 新增电脑控制台和手机选择页的连接自检、安全试电与停止输出入口，使正式游玩前可直接验证 App 绑定、浏览器连接、A/B 通道和限幅状态。
- 新增手抖挑战、保持角度、摇骰子对决和极速角子机的输出节奏参数；持续型玩法可设置短促输出后的休息时间，骰子可设置每下间隔，角子机可设置电完休息。
- 新增极速角子机“满槽后压力”设置，可选择满槽惩罚结算后清空压力或保留 100%，避免默认循环触发不透明。
- 新增摇骰子对决真实骰子点阵显示，玩家与对手各 3 颗骰子从数字文本升级为可视骰面，结算时可直接核对 6 颗骰子和双方总分。
- 新增手抖挑战、保持角度和极速角子机的危险态视觉与本机震动反馈；偏离安全区、角度持续偏离、角子机高压和开奖结果现在有独立颜色或震动节奏，降低玩家错过触发前兆的概率。
- 新增 `USER_GUIDE.md` 用户手册，集中说明启动流程、手机 HTTPS 证书安装、App 绑定、四款游戏玩法、A/B 通道、紧急停止、安全事项和常见问题，降低首次使用和现场排障成本。
- 新增本地 HTTPS/WSS 模式，启动时自动检测当前局域网 IP 并签发服务器证书，使依赖传感器的玩法可通过 HTTPS 请求动作与方向权限。
- 新增电脑控制台“手机证书”页，提供 iPhone 描述文件二维码、Android `.cer` 二维码、证书 SHA-256 指纹、签发 IP 和到期时间，减少 iOS/Android 安装证书与排查 IP 不匹配的操作成本。
- 新增第 4 款独立小游戏“极速角子机”，支持 3 轮 Emoji 高频开奖、彩色压力进度条、满槽惩罚和自动连转冷却，避免改动原有快节奏摇骰子玩法。
- 新增极速角子机“没中奖轻电”开关和轻电时长设置，使“没中奖轻电强度”只在空转轻惩罚开启时生效，避免满槽规则下参数无实际用途。
- 新增每个游戏 3 项输出通道设置，可选择只用 A、只用 B 或 A+B 同时输出，并支持 B 通道按 A 强度比例降低或保持一致。
- 新增极速角子机 14 个独立设置字段，覆盖轻电、满槽强度、满槽时长、开奖速度、自动连转、压力涨跌、中奖率和三个 7 特殊规则，使压力条节奏和惩罚强度可按玩法单独调校。
- 新增手机游戏选择页安全说明、A/B 通道说明和 13 项技术状态字段，使手机端可直接核对端口、连接、延迟、强度、限幅与设备电量接入状态。
- 新增手抖挑战、保持角度、摇骰子对决主要玩法字段的小字说明，明确每个强度、容错、灵敏度、时长和难度参数的用途，降低误调参数的概率。
- 新增电脑控制台的手机游戏端连接状态与设备电量接入状态展示；当前桥接库未暴露电量读取时显示“未接入”，避免把未知状态误报为正常电量。
- 新增手机端三段式游戏流程：选择游戏、独立设置、开始游玩，四个游戏的强度、容错、灵敏度、时长和压力参数互不污染。
- 新增游戏参数本地持久化，刷新页面后仍保留每个游戏的独立配置，减少重复调参成本。
- 新增手抖挑战与保持角度的姿态校准入口，并在开始游戏时自动兜底校准，降低初始握持角度导致的误触发。
- 新增游戏端 WebSocket 运行 token，手机二维码携带 `ws` 端口和 token，端口自动递增后仍可正确连接。
- 新增手机端页面隐藏、离开、刷新和冻结时的紧急停止逻辑，并请求屏幕常亮以降低游戏中途息屏概率。
- 新增摇骰子对决简化豹子规则，任意一方摇出豹子时按“点数 × 豹子倍率”换算惩罚次数，并受单局最多次数限制。
- 新增每个游戏游玩页顶部说明，使玩家进入玩法后能立即看到结算规则和触发条件。

### Changed
- 电脑控制台从 2 个主要连接二维码扩展为设备 App、浏览器游戏和 Android APK 3 个入口，网页版与 APK 继续共用同一套游戏逻辑。
- 将项目、网页、证书、环境变量、下载文件和浏览器设置命名统一为 `GameBridge for Fun` 品牌体系，清除全部面向用户的第三方产品品牌字样；旧根证书需删除并重新安装，新版网页游戏参数会按新命名空间重新初始化一次。
- 调整摇骰子对决的连续输出队列，每下输出结束后按设置等待，页面同步估算本局最长输出时间，降低“连续多下叠太密”的风险。
- 调整极速角子机的轻电、满槽电和自动连转节奏，触发输出后必须等输出结束并完成电完休息，再允许下一局继续。
- 将 HTTPS 证书策略从固定 `192.168.0.14` 改为启动时自动检测局域网 IP；IP 变化时只重签服务器证书，正常情况下手机不需要重新安装根证书。
- 后端状态广播从仅电脑控制台扩展到电脑和手机游戏页两端同步，技术排障入口从 1 个页面扩展为 2 个页面。
- 简化摇骰子对决结算逻辑，改为“输几点就电几下、每下固定强度和时长”，减少强度区间和时长区间造成的理解成本。
- 重写手机选择页、设置页和当前参数摘要的主要说明文案，将硬件术语压缩为玩家可直接理解的 A/B 输出、每下强度、没中奖轻电和满槽电击。
- 将第三方桥接依赖锁定到已验证的 `1.1.0`，降低上游接口变更导致运行结果漂移的风险。

### Fixed
- 修复第三方桥接依赖 `1.1.0` 限幅字段名读取错误；后端现在读取真实的 `a_limit/b_limit`，在任一所选通道限幅未返回或为 0 时拒绝整次输出，消除错误回退到强度 200 及 A+B 静默退化为单通道的路径。
- 修复输出失败或紧急停止时 A/B 清理可能交叉执行、单路清理失败后跳过另一路的问题；现在输出与停止共用 1 把串行锁，并保证 2 个通道分别尝试清理。
- 修复摇骰子对决只处理玩家豹子、不处理对手豹子的问题；现在你或对手任意一方豹子都会直接按倍率结算，双方同时豹子时按较大点数算一次。
- 修复 iPhone 上传感器权限请求不完整、手抖挑战和保持角度无明确授权反馈的问题；同时放开游玩页纵向滚动，并补齐摇骰子对决的对手三颗骰子显示。
- 修复 B 通道选择“跟 A 一样”时仍显示比例滑块的问题，并在设备 App 未绑定时明确提示“未输出”，避免玩家误以为已经实际触发。
- 修复无 token 打开手机游戏页被拒绝时后端打印 WebSocket 异常栈的问题，使无效访问仍被拒绝但服务日志保持可读。
- 修复未绑定设备 App 时游戏页断开仍尝试发送硬件停止指令的问题，减少无设备测试和服务退出时的误导性错误日志。
- 修复手机游戏页全局禁用触摸滚动导致选择页和长设置页难以滚动的问题，并在页面切换时自动回到顶部，避免返回按钮出现在视口外。
- 修复本机局域网 IP 可能被识别为 `198.18.*` 虚拟网卡地址的问题；二维码现在优先使用 Wi-Fi/局域网私有地址，降低手机扫码后打不开页面的风险。
- 修复本地 `qrcode.min.js` 文件语法损坏导致电脑后台无法绘制二维码的问题，并提高二维码容量配置以容纳 App 绑定长链接。
- 修复二维码库初始化失败时阻断控制台 WebSocket 连接的问题；即使二维码无法绘制，页面也会显示可手动输入的绑定数据和游戏地址。
- 修复电脑后台二维码区域在 WebSocket 端口变更、浏览器缓存旧脚本或二维码生成异常时静默空白的问题；控制台现在会使用启动时的真实 WS 端口并显示离线/生成失败提示。
- 修复 V3 脉冲数据结构错误，将无效四元组改为第三方桥接库接受的双四元组波形，恢复实际脉冲下发链路。
- 修复 App 绑定二维码格式，改为使用库内 `get_qrcode` 生成，避免扫码入口与当前库协议不一致。
- 修复保持角度游戏启动即惩罚的问题，加入传感器就绪判断、启动缓冲和持续偏离判定。
- 修复传感器监听重复绑定、骰子手动摇号可连点、停止输出后仍可被运动传感器触发的问题。
- 修复 README 指向已删除硬件协议目录的问题，使项目说明与当前仓库结构一致。
- 修复手机游戏端断开连接后服务端没有立即兜底停止输出的问题，降低上滑回桌面或浏览器杀后台时残留输出风险。

### Security
- Android WebView 禁止文件、内容、地理位置、Cookie、弹窗和混合内容访问；原生传感器仅向经过校验的游戏页单向注入数值，不暴露 JavaScript 系统桥接接口。
- APK 将明文局域网权限限制在私有 IPv4、固定 `/static/game.html` 路径、有效端口和一次性 token，拒绝公网及任意页面加载；切后台、锁屏、返回连接页或 WebView 崩溃时停止传感器并断开游戏连接。
- 将 HTTP 公开范围从整个项目目录收紧为 `static/` 与 2 个可安装证书文件，阻断源码、`.git`、私钥、目录列表、路径穿越和符号链接越界读取。
- 将 `/console` WebSocket 限制为本机连接并校验本机页面来源，阻断同网段设备读取游戏 token 后绕过游戏端鉴权；游戏 token 比较改为恒定时间比较。
- 将网页消息上限压至 4 KiB、单连接等待队列压至 8 条，并将硬件输出限制为最多 1 个活动任务，避免超大消息和长时输出请求形成无界内存队列。
- 新增安全试电后端硬限制，测试输出最高强度 30、最长 1 秒，并设置约 1.5 秒冷却，防止前端参数被篡改后把测试入口变成高强度输出入口。
- 将本地根证书默认有效期设为 90 天、服务器证书默认有效期设为 7 天，并在旧版 10 年根证书残留时自动重建，降低证书私钥泄露后的长期滥用窗口。
- 后端新增强度 0-200、单次时长 100-60000ms、连续惩罚 0.22s 间隔的硬限制，降低局域网刷包导致的过量输出风险。
- 新增 `stop_shock` 停止输出路径，返回列表或点击停止输出时会清空 A/B 两路波形并尝试将强度归零。
- 将后端单次惩罚时长上限扩展到 60 秒，同时继续保留服务端硬限幅，避免前端异常输入无限延长输出。

## [1.0.0] - 2026-07-06

### Added
- 新增一键双击启动脚本 `start.command`，实现 macOS 环境下的依赖自检与服务自动拉起。
- 新增局域网 Web/WebSocket 控制台，支持展示 App 连接二维码、局域网 IP 与延迟 RTT 监控。
- 新增三个极简 Canvas/Web 网页小游戏（手抖挑战、保持角度、摇骰子对决）。
- 前端游戏集成 Web Audio API 骰子撞击音效合成器与 `navigator.vibrate` 物理震动反馈。
- 引入本地 `qrcode.min.js`，实现离线/弱网二维码极速渲染。
- 后端 Python 异步服务端，实现多服务端口防冲突分配与 V3 脉冲波形下发逻辑。

### Removed
- 删除了与郊狼脉冲主机无关的其他硬件协议文档（爪印无线按钮传感器、灵猫边缘控制传感器、负鼠振动控制器）。
