# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [1.0.0-beta.1] - 2026-07-23

### Added

- 新增手抖挑战、保持角度、摇骰子对决和极速角子机 4 种玩法，电脑控制台、手机网页和 Android APK 共用同一套可测试规则。
- 新增游戏默认、按时长随机与 16 种经典波形共 18 个输出感觉选项；短任务自动压缩完整波形，避免 0.5 至 1 秒输出只播放缓慢起始段。
- 新增 DG-LAB V4 Socket 本地网关，同时识别 `COYOTE_020` 与 `COYOTE_030`，按设备类型发送 V2 三字节或 V3 八字节波形。
- 新增 macOS 双击启动体检，覆盖系统、Python 3.9+、11 项运行文件、依赖、网络、OpenSSL、证书和 APK 完整性。
- 新增 Android 15+ 原生传感器与受限 WebView，提供常驻原生停止入口，切入后台或锁屏时主动断开并请求停止。
- 新增正式 Android 签名创建、发布构建和只读验签入口；版本、签名、SHA-256 与 APK 内部清单均进入发布检查。
- 新增 PolyForm Noncommercial 1.0.0 许可、第三方组件说明、贡献说明和安全问题报告流程，使公开源码能够在非商业目的下修改和再分发。
- 新增版本与发布卫生回归测试；Python 自动化用例由 72 项增加到 83 项，另保留 19 项浏览器规则测试和 12 项 Android 单元测试。

### Changed

- 首次公开版本统一为 `1.0.0-beta.1`；Python 元数据、uv 锁文件、Android `versionName`、APK 文件名、用户手册和页面状态使用同一版本来源。
- Python 后端集中到 `server/`，网页保留在 `static/`，Android 与测试继续独立分区；完整源码、锁定依赖和构建脚本全部保留。
- 电脑控制台改为状态、证书和技术信息 3 个页签，手机端改为选择、设置和游玩 3 段流程，4 种玩法首次可见设置均压缩到最多 1.80 屏。
- 设备桥接从旧 V3 第三方依赖改为项目内 V4 实现，运行依赖从 2 个缩减为固定版本的 `websockets` 1 个。
- 安全试电固定使用立即有感波形，默认强度由 5 调整为 15；后端最高强度 30、最长 1 秒和 1.5 秒冷却保持不变。
- README 改为面向公开 Beta 的项目说明，只保留定位、4 种玩法、启动、APK、证书、安全、二次开发和许可信息。
- HTTPS 私钥从源码目录迁移到 `~/Library/Application Support/GameBridge for Fun/`，更新或移动项目文件夹不再复制私钥。

### Removed

- 移除 4 份逐字复制的上游 V2/V3 协议文档，改为 1 份官方资料索引，减少公开仓库中的授权歧义且不影响运行代码。
- 公开 APK 不再使用 Android Debug 证书，调试构建也不再覆盖 `APK/` 中的正式 Beta 安装包。

### Fixed

- 修复旧 V3 二维码只能添加 3.0 设备的问题，绑定、设备发现、强度和波形下发全部使用 V4 通道。
- 修复重复发送平直帧导致 2.0 设备低强度下几乎无感的问题，0.3 秒默认任务现在包含 3 个有效变化帧。
- 修复 App 限幅未读取时可能回退到协议最大强度的问题；任一所选通道状态未知、限幅为 0、静音或过热时整次输出为 0 条。
- 修复页面关闭、锁屏、弱网半断开、传感器停更和多页面同时断开时可能残留输出的问题，前后端统一取消预约任务并逐路清空 A/B。
- 修复角子机 `7️⃣` 与自带 777 图案含义混淆的问题；特殊事件只在三个图标全部为 `7️⃣` 时成立。
- 修复浏览器设置损坏、重复快速点击、迟到 WebSocket 回调和骰子并行结算可能绕过正常流程的问题。

### Security

- 非零强度全部使用带自动到期时间的 V4 临时任务；电脑意外退出后，App 仍会在请求时长加 0.8 秒安全余量内归零。
- 输出期间要求所属页面持续发送应用层心跳；长任务 3.5 秒无心跳、持续玩法 0.75 秒无新脉冲或传感器 1.6 秒无数据时强制停止。
- 后端强制限制强度 0 至 200、单次时长 100 至 60000 毫秒、最短脉冲间隔 0.22 秒，并拒绝重叠任务和未知设备型号。
- 游戏端使用每次启动随机 token；电脑控制台只接受本机来源，手机状态包不包含 App 配对二维码、运行 token 或证书详情。
- HTTP 公开范围限制为网页资源和 2 个根证书下载文件，拒绝源码、Git 数据、目录列表、私钥、路径穿越与符号链接。
- 根证书有效期限制为 90 天、服务器证书限制为 7 天；证书与私钥目录权限收紧为当前 macOS 用户可访问，符号链接证书在启动前拒绝使用。
- Android WebView 拒绝公网、文件、Cookie、弹窗、混合内容和非白名单资源；公开 APK 使用独立 4096 位 RSA 正式签名。
